# 2026-06-06 工作日志

## C/ai.html 侧边栏子内容展开后局部滚动

**需求**：三个一级菜单（我的人类伙伴、我的AI团队、组织栏）的子内容列表展开后，超出浏览器可视区域时，子内容区域本身支持局部滚动查看。

**修改内容**：
- 将 `.sidebar-menu.expanded.overflow .submenu-scroll-area` 的滚动样式改为 `.sidebar-menu.expanded .submenu-scroll-area`
- 添加 `max-height: 40vh`，让展开后的子内容列表超出 40vh 时自动出现滚动条
- 去掉对 JS `.overflow` 类的依赖，展开即滚动
- 鼠标悬停显示滚动条（scrollbar-width: none → hover 时显示）
- 撤回之前错误的全侧边栏滚动方案（`sidebar-scroll-content` 包裹层已移除）
