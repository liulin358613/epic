# 2026-06-01 工作日志

## C/org-manage.html 布局调整

### 修改内容
将下半部分"组织成员"容器改为占据剩余屏幕高度的局部滚动布局：

1. **body 改为 `overflow: hidden`**：防止整页滚动，改为内部局部滚动
2. **外层 `div[padding:16px]` 增加**：`height: 100vh; display: flex; flex-direction: column; box-sizing: border-box; overflow: hidden;`
3. **组织信息卡片**：加 `flex-shrink: 0; overflow-y: auto; max-height: 55vh;`（高度不固定，内容超出时自身可滚动）
4. **组织成员卡片**：加 `flex: 1; min-height: 0; margin-bottom: 0;` 撑满剩余高度
5. **CSS 样式更新**：
   - `.org-member-card`：移除 `flex: 1`（改由 inline style 控制），加 `min-height: 0`
   - `.org-tree-panel` / `.org-tree-scroll` / `.member-list-panel` / `.member-list-scroll`：均加 `min-height: 0` 确保子容器可压缩
   - `.pagination-bar`：加 `flex-shrink: 0` 固定在底部
6. 修复了一个 HTML 结构错误：编辑时意外将 section-content 内容混入 section-header，已恢复正确嵌套

## C/ai.html 组织icon图标 → 打开组织详情Tab

### 修改内容
点击侧边导航组织栏（组织二级菜单）中的**组织icon图标**（org-structure-icon SVG），在预览区打开新tab-组织详情，内容引入 org-manage.html。

1. **`openOrgDetailTab()` 函数**（新增）：检查是否已有"组织详情"tab，若已存在直接激活，否则新建；创建后关闭下拉菜单
2. **`org-structure-icon` 点击事件**（第9919行）：原来仅 `return`（阻止组织切换），现改为调用 `openOrgDetailTab()` 并 `stopPropagation()`
3. **`activateTab` 分支**：新增 `'组织详情'` 分支，加载 `org-manage.html` 到 iframe
4. **`updatePreviewPadding`**：将"组织详情"加入零内边距列表（与"文件管理"/"项目管理"一致）
