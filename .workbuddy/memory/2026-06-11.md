# 2026-06-11 工作日志

## 重构 B/add-sub-org.html

完整重构「添加下级组织」页面，主要改进：

1. **步骤指示器**：连接线从 CSS `::after` 伪元素改为独立 `<div class="step-line">`，精确对齐步骤圆圈中心；完成步骤连接线变绿色
2. **表单布局**：label 改用 `.field-label` + `.field-control` 水平对齐，label 宽度统一 110px，右对齐，间距规范
3. **输入框**：统一高度 36px，focus 状态蓝色边框+淡蓝阴影
4. **按钮**：下一步/提交改为实心蓝色主按钮（`btn-primary`），上一步为默认边框按钮
5. **权限面板**：三列 flex 布局，各列独立滚动，max-height 580px；数据权限/字段权限表格样式统一
6. **响应式**：900px 以下权限面板纵向堆叠，768px 以下表单行纵向排列
7. **CSS 变量**：全局用 `:root` 变量管理颜色/间距/阴影，删除 tailwind CDN 依赖
8. **viewport**：移除 user-scalable=no，允许用户缩放
