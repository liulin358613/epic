# 2026-06-11 工作日志

## 重构 B/add-sub-org.html

完整重构「添加下级组织」页面，主要改进：

1. **步骤指示器**：连接线从 CSS `::after` 伪元素改为独立 `<div class="step-line">`，精确对齐步骤圆圈中心；完成步骤连接线变绿色
2. **表单布局**：label 改用 `.field-label` + `.field-control` 水平对齐，label 宽度统一 110px，右对齐，间距规范
3. **输入框**：统一高度 36px，focus 状态蓝色边框+淡蓝阴影
4. **按钮**：下一步/提交改为实心蓝色主按钮（`btn-primary`），上一步为默认边框按钮
5. **权限面板**：三列 flex 布局，各列独立滚动，max-height 580px；数据权限/字段权限表格样式统一
6. **响应式**：900px 以下权限面板纵向堆叠，768px 以下表单行纵向排列
7. **CSS 变量**：全局用 `:root` 变量管理颜色/间距/阴影，删除 tailwind CDN 依赖
8. **viewport**：移除 user-scalable=no，允许用户缩放

## 重构 A/data-perm.html 排版与样式

全面调整「组织类型」管理页面的布局、间距、响应式和浏览器兼容性：

1. **CSS 设计系统**：建立完整 `:root` 变量体系（`--sp-*` 间距 / `--color-*` 颜色 / `--radius-*` 圆角 / `--shadow-*` 阴影 / `--font-*` 字号 / `--sidebar-width` 侧边栏宽度）
2. **布局优化**：`body` 使用 `display:flex; flex-direction:column; overflow:hidden` 填满视口；`main-content` `gap` 从 4px 改为 16px；`content-body` 显式 `display:flex; flex-direction:column`
3. **间距规范化**：全局 padding/margin/gap 统一使用变量；表单行 `gap` 32px、权限面板间距 16px、内容卡片 `gap` 16px
4. **响应式断点**：5 级断点 - 默认(1440) → ≤1280（sidebar 240px）→ ≤1024（sidebar 200px + 表单竖向）→ ≤768（最小布局）→ ≥1920 → ≥2560（超宽屏优化）
5. **浏览器兼容**：保留 Firefox `scrollbar-width:thin`、IE/Edge `-ms-overflow-style` 兼容规则；select 添加 `-webkit-appearance:none; -moz-appearance:none`
6. **颜色统一**：全局 inline style 中的硬编码色值（`#e5e7eb`, `#e2e8f0`, `#f1f5f9`）替换为 CSS 变量；表格 padding 从 14px 调整至 12px 14px
7. **冗余清理**：移除 JS 模板中 org-role 标签页的嵌入式 `<style>` 块，对应样式上移至主 CSS 区域

## 修复组织类型页权限面板局部滚动

`.content-right` 缺少 `display: flex; flex-direction: column;`，导致其子元素（flex:1 的中间容器）在 block 上下文中无法继承高度约束，三个权限面板（功能权限/数据权限/字段权限）的 `overflow-y: auto` 内层无法触发滚动。添加后高度约束链完整传递，内容超出时面板内局部滚动生效。

## 修复组织角色"权限设置"选中效果

A/data-perm.html 组织角色tab中，点击设置icon二级菜单的【权限设置】后，当前角色项缺少选中高亮效果。修改 `setRolePermissions(roleId)` 函数：
- 先移除所有 `.role-item` 的 `selected` 类
- 给当前点击的角色项添加 `selected` 类（蓝色背景 #e0f2fe + 蓝色边框）
- CSS 新增 `.role-item:not(.role-item-all)` 默认透明 border，避免选中时布局跳动
