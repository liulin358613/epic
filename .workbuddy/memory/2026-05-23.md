# 2026-05-23 工作记录

## 在C文件夹创建项目详情页面

- 文件：`/Users/epic/Desktop/爱沛柯尔/html原型页面/C/project-detail.html`
- 页面类型：项目详情页面（单个项目）
- 设计规范：沿用C文件夹现有规范（响应式字体、:root变量、卡片式布局、蓝色主题#0051D9）
- 页面包含模块：
  1. 项目概述（基础信息卡片 + 统计数字 + 项目描述）
  2. 项目总体进度甘特图（表格形式，含进度条、状态标签）
  3. 项目SOP流程（时间线形式，8个节点，含说明/负责人/状态）
  4. 项目反馈（留言列表形式）
  5. 现场图片/视频（网格布局，含视频播放标记）
  6. 成员组织架构（层级结构图）
  7. 补充信息：项目里程碑、风险登记册、项目文档列表
- 响应式：支持从 1440px 到 600px 多个断点
- 顶部导航：返回按钮 + 标题 + 操作按钮

## SOP流程重构（并行节点支持）

- 根据用户提供的参考图片，重新设计SOP流程布局
- 主流程节点横向排列（8个节点）
- 节点2（方案设计）下方添加2个并行子节点：系统架构设计、设备选型评估
- 节点4（系统开发）下方添加2个并行子节点：MES核心模块开发、ERP接口联调
- CSS类名从 `.sop-node` 改为 `.sop-column`，新增 `.parallel-nodes` 和 `.parallel-node`
- 连线风格：主节点之间横向连线（2px，var(--border)），主节点到并行节点竖向连线
- 并行节点样式：白色背景+边框，小一号的圆圈和文字
- 清理了残留的 `.node-dot::after` CSS代码

## SOP流程精简（去除子级节点+圆圈优化）

- 删除所有 `sub-nodes`、`parallel-nodes` 及对应CSS，SOP简化为8个主节点横向平铺
- 编号修正：2-1→2，4-1→4
- 节点圆圈从1.5rem加大到2rem，内部显示步骤数字
- 已完成状态圆圈内白色字，未开始/取消灰色字
- 去掉卡片标题内的蓝色圆圈(step-num)，数字统一移至节点圆圈内
- 横向连线位置调整为top:2rem适配更大圆圈

## SOP连线对齐优化

- 横向连线方案改为 `.sop-timeline::before` 贯穿线，从第一个圆圈圆心到最后一个圆圈圆心
- 不再使用每个 column 的 `::after` 画段横线，消除了列间距导致的断开问题
- 竖向连线 `left: calc(1.75rem - 1px)` 精确对齐圆圈圆心
- 圆圈 z-index:1 覆盖横线 z-index:0，确保视觉完整

## SOP连接线修复（竖折连线间隔+贯穿线出头）

1. **单卡片节点连接线间隔消除**：`::after` 垂直线高度从 `1rem` 改为 `calc(2rem - 1px)`，延伸到 `::before` L折线 border-left 起始位置，消除1rem空白
2. **多卡片节点贯穿线不超出**：`.node-connector` 移除 `bottom: 0.5rem`，改为JS动态计算高度（`updateNodeConnectors()`），精确停在第二卡片L折线起点
3. **X坐标对齐修正**：`::before`/`::after` left 从 `calc(-0.75rem - 1px)` → `calc(-0.625rem - 1px)`，与圆圈中心X(1.875rem)精确对齐
4. **sibling卡片 `::after` 隐藏**：`display: none`，由贯穿线替代其垂直线功能

关键计算：圆圈中心X = 0.125rem + 0.75rem + 1rem = 1.875rem；卡片margin-left=2.5rem，所以伪元素left = -0.625rem

## SOP连接线二次修复（贯穿线Y坐标+JS计算修正）

1. **贯穿线起点修正**：`.node-connector` 的 `top` 从 `calc(0.5rem + 2rem)` 改为 `2rem`，因为 `0.5rem` 是 timeline 的 padding-top，而 connector 在 column 内（column 无 padding-top），不应包含 timeline 的 padding
2. **JS计算修正**：`updateNodeConnectors()` 中 `targetY` 去掉多余的 `cardPaddingTop`。`lastCard.offsetTop` 已经是卡片 padding edge 相对于 column 的位置（无 border 时），再加 `cardPaddingTop` 会导致贯穿线延伸过头 `0.75rem`
3. **有sibling时隐藏第一卡片 `::after`**：`.sop-column.has-sibling > .node-card:first-of-type::after { display: none }`，避免第一卡片自己的垂直线与贯穿线重叠冲突

修正后逻辑：
- 单卡片节点：`::after` 垂直线 + `::before` L折线（各自独立）
- 多卡片节点：一条 `.node-connector` 贯穿线替代所有垂直线段，两个卡片都只画 `::before` L折线

## ai.html 整体字体/间距/行高响应式适配

- 文件：`/Users/epic/Desktop/爱沛柯尔/html原型页面/C/ai.html`
- 根字体从固定 `16px` → `clamp(14px, calc(12px + 0.28vw), 18px)`，随视口宽度自适应缩放
  - 1366px屏幕：~15.8px；1440px：~16px；1920px：~17.4px；≥2143px：18px（上限）
- body 新增 `line-height: 1.6;` 全局行高基线
- 全部 `font-size: Xpx;` → `rem` 单位（10/11/12/13/14/15/16/18/20/24/28/40/48px）
- 严格遵循最小改动原则：仅修改字体相关属性，未触碰任何JS逻辑、事件处理、布局尺寸、颜色、内边距/外边距

## ai.html chat-input-wrapper 边距调整

- 原始 `padding: 0 10%`（百分比，缩放后边距过大）
- 第一次改为 `padding: 0`（过头，输入框贴边无间距）
- 最终 `padding: 0 1.5rem`（固定值，左右约24px，缩放后不变）
- 再次改为 `padding: 0 2.5rem` → 用户要求恢复 → 回退为原始 `padding: 0 10%`

## ai.html 左侧栏新增【我的组织】、【我的项目】

- 在 sidebar-top（项目经理）下方、我的AI团队上方，插入两个新的 `.sidebar-menu` 区块
- 样式完全复刻【我的AI团队】：menu-item + collapse-icon + 空 team-members 容器
- 【我的组织】图标：房子(home)；【我的项目】图标：文件夹(folder)
- JS 添加 `initCollapse('orgMenu', 'orgMembers')` 和 `initCollapse('projMenu', 'projMembers')`
- 下级内容为空，等用户后续填充

## ai.html 我的组织 → 组织名称 + hover图标

- 文字从「我的组织」改为「组织名称」
- hover 时显示两个操作图标：组织架构（层级图）和设置（齿轮）
- 图标14px，灰色默认，hover蓝色；hover时折叠箭头隐藏
- CSS 新增 `.org-actions` 样式块，HTML 在 menu-item-content 与 collapse-icon 之间插入 org-actions div

## ai.html 组织分割线+改名+去折叠

- 我的组织上方添加分割线：`<div style="border-top: 1px solid var(--border-gray); margin: 0.5rem 0;">`
- 组织名称改为「东莞嘉东科技有限公司」
- 删除 collapse-icon（展开/折叠箭头）

## ai.html 控制台报错修复

- **错误**：`Cannot read properties of null (reading 'classList')` at ai.html:8251 和 8267
- **根因**：`initCollapse()` 函数中 `menu.querySelector('.collapse-icon')` 返回 null（orgMenu 没有折叠图标子元素），直接调用 `.classList.remove('collapsed')` 导致崩溃
- **修复**：在 `initCollapse()` 中增加 `if (!menu || !members) return;` 顶层守卫，以及所有 `icon` 操作前加 `if (icon)` 空值检查
- **影响范围**：仅修改 `initCollapse` 函数（约第8243行），其他代码未动

## ai.html 组织切换下拉按钮

- 在「东莞嘉东科技有限公司」名称右侧添加下拉箭头按钮
- 点击展开组织列表，可选：东莞嘉东科技、深圳智造科技、广州自动化设备
- 点击选项后更新当前组织名称，箭头自动收起
- CSS: `.org-switch-dropdown` / `.org-switch-btn` / `.org-switch-menu` / `.org-switch-item`
- JS: `toggleOrgSwitch()` + 点击外部关闭 + 选项点击切换
- 仅改了 orgMenu 的 menu-item-content 内部HTML、新增CSS样式块、新增JS函数，其他未动

## ai.html 组织切换下拉图标调整（靠右+加大）

- SVG箭头从 12×12px → **16×16px**，stroke-width 从 2 → **2.5**
- 内联flex容器加 `width: 100%`，配合已有 `margin-left: auto` 将图标推到最右侧
- 仅修改了SVG尺寸和父容器宽度属性，其他未动

## ai.html 组织名称字体加大加粗

- 组织名称 `<span>` 加内联样式 `font-size: 1rem; font-weight: 600;`
- 组织图标从 16×16px → **18×18px**，与加粗文字协调
- hover时 `.menu-item` 无放大效果（仅背景色变化），无需修改

## ai.html 组织切换下拉样式迭代（图标旋转 + 菜单缩进）

- 点击组织切换按钮展开菜单后，下拉箭头图标从向下旋转为向右（`transform: rotate(-90deg)`）
- CSS: `.org-switch-btn.open svg { transform: rotate(-90deg); }`，已有 `transition: transform 0.2s`
- JS: 展开时 `btn.classList.add('open')`，关闭时 `removeClass('open')`
- 组织菜单栏（`orgMenu`）下的【我的项目】【我的人类伙伴】【我的AI团队】添加 `org-submenu` 类
- 缩进方式：**用 `margin-left` 而非 `padding-left`**，确保 hover 背景框不铺满整个宽度
- CSS: `.org-submenu .menu-item { margin-left: 1.5rem; }`
- `#teamMembers2/1 .team-member { margin-left: 2rem; }` 团队成员再缩进一级
- `#projMembers .history-item { margin-left: 2rem; }` 历史对话作为我的项目子菜单也缩进

## ai.html 历史对话移入【我的项目】子菜单

- 删除 sidebar 中独立的 `<div class="history-title">历史对话</div>` 和整个 `history-list` 块
- 历史对话项（`.history-item`）移入 `projMembers`（`我的项目`的 team-members 容器）
- 历史对话作为"我的项目"展开后的子级内容显示

## ai.html 侧边栏登录栏固定底部（Flex方案）

- **问题**：`position: sticky; bottom: 0` 在菜单全部收起（内容高度 < 容器高度）时失效，登录栏被移上来
- **方案**：将 `sidebar` 改为 flex column 布局，`sidebar-top` 设 `flex: 1; overflow-y: auto` 负责内容滚动，`sidebar-login-bar` 设 `flex-shrink: 0` 始终在底部
- `sidebar` CSS 变更：去掉 `overflow-y: auto`，加 `min-height: 0`
- `sidebar-top` 新增：`flex: 1; overflow-y: auto; min-height: 0`
- `sidebar-login-bar` CSS 变更：去掉 `position: sticky; bottom: 0`
- 滚动条样式从 `.sidebar` 移到 `.sidebar-top`
- 修复过程有反复（sticky → flex → sticky → flex），最终 flex 方案正确

## ai.html 侧边导航三段式布局修复 + main-container-inner 添加 + 内容区恢复

### 问题
用户反馈两个问题：
1. 侧边导航排列不对（需按三段式：顶部固定/中间可滚动/底部固定登录按钮）
2. 内容页面没展示了（右侧对话区/预览区不显示或崩了）

### 修复内容

**1. sidebar-fixed-top 未关闭（结构性 bug）**
- `<div class="sidebar-fixed-top">` 在 L3800 打开后从未关闭，导致 scroll-area 被错误嵌套在内部
- 在 L3891 分割线后插入 `</div><!-- /sidebar-fixed-top -->` 关闭标签
- 诊断方式：Python 逐行追踪 div 嵌套深度发现 depth 异常

**2. 移除 .main-container 的 justify-content: center**
- 该属性将 sidebar+chat-area 整体居中而非左对齐填充，导致布局偏移

**3. 新增 main-container-inner 包裹层**
- CSS（L888）和 JS（多处 querySelector）都依赖 `.main-container-inner`，但 HTML 中缺失
- 在 `<div class="main-container">` 内部新增包裹层，将 sidebar/chat-area/resizer/preview-area 全部放入
- 开标签在 L3797，闭标签在 L4919

### 验证结果
- DOM 结构完整验证通过：depth 从 1→0 全程无负值
- 三段式结构确认：sidebar-fixed-top(d=3固定) / sidebar-scroll-area(d=4可滚动) / sidebar-login-bar(d=4固定底部)
- main-container-inner 正确包裹了所有子元素
