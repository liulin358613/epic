# 2026-06-04 工作日志

## C/ai.html 侧边栏布局优化

**需求：**
1. 菜单标题（我的人类伙伴、我的AI团队、组织栏）始终可见
2. 子内容过多时局部滚动
3. 其他同级栏目往下撑直到个人信息栏
4. 个人信息栏固定底部始终可见

**改动：**
- CSS `.sidebar-fixed-top`：从 `flex-shrink: 0` 改为 `flex: 1; overflow-y: auto; min-height: 0; padding-bottom: 60px`，使整个内容区自适应高度并整体可滚动，留出 login-bar 空间
- 新增 CSS 类 `.section-scroll-area`：`overflow-y: auto; max-height: 35vh`，用于每个菜单栏的子内容局部滚动
- HTML 三处子内容包裹 div：将内联 `style="overflow-y: auto; max-height: calc(100vh - 380px); scrollbar-width: none;"` 替换为 `class="section-scroll-area"`
- `sidebar-login-bar` 保持原有 `position: absolute; bottom: 0`，固定底部不变
