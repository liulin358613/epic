# 2026-05-30 工作日志

## A/user-list.html 组织下拉筛选器改造

将搜索区的组织 `<select>` 替换为自定义可搜索树形下拉选择器：
- 新增 CSS：.org-select-wrapper/trigger/dropdown/search/list/item 等完整样式，含展开箭头旋转、搜索高亮、选中态、层级缩进、tree-line 树形符号
- 新增 HTML：自定义下拉组件（触发器 + 下拉面板含搜索框 + 列表容器）
- 新增 JS：orgTreeData 数据（15个组织，其中10个有子级部门，共约35个选项）、renderOrgTree 搜索过滤+树形渲染、toggleOrgExpand 展开/收起子级、selectOrg 选择、点击外部关闭
- 搜索功能：输入关键词实时过滤，匹配项高亮，子级匹配时自动展开父级
- 层级展示：父级有 ▶ 展开按钮，子级用 ├─ / └─ 树形连接符缩进显示
- 其他部分（等级筛选、类型筛选、搜索按钮、重置按钮、表格、弹窗）均未修改

## A/user-list.html 组织下拉选择器样式调整

根据参考截图重新调整组织下拉选择器样式：
- 去掉展开/折叠功能，所有子级直接平铺展示（参考截图中无折叠按钮）
- 每项显示格式统一为：`组织名称（人数人）`，如`东莞嘉东科技有限公司（88人）`
- 子级部门用 `├─` / `└─` 树形连接符缩进展示，无图标无展开按钮
- 选中项背景色改为浅蓝色 `#e8f2ff`，文字改为蓝色 `#2563eb`
- 数据扩充至17个组织，新增
