# 2026-06-12

## B/index.html - 个人中心弹窗美化

- 将「个人中心」弹窗的所有表单字段改为**标题与内容同行**布局（label+value 水平对齐）
- 新增 `.pf-row` / `.pf-label` / `.pf-value` CSS 类，label 固定宽64px右对齐，value 区域 flex:1
- 头像区域改为左图右文横排，去掉居中大头像
- 密码行改为输入框+按钮横排，修改密码展开区内也改同行排列
- 弹窗宽度改为 `clamp(480px, 50vw, 600px)` 响应式适配
- 添加 `@media (max-width:540px)` 小屏折行降级处理
- 关闭按钮美化为圆角方块样式，增加 hover 态
- 整体 modal-body 增加 max-height+scroll，避免小屏遮挡
